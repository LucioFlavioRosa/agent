# workflows.yaml
# Atenção: Este arquivo agora contém apenas workflows NÃO relacionados ao Azure Board.

workflows:
  modernizacao:
    steps:
      - agent: revisor
        params:
          ...
      - agent: processador
        params:
          ...
      # outros workflows mantidos
  implementacao:
    steps:
      - agent: processador
        params:
          ...
  # Removidos:
  # - criacao_epicos_azure_devops
  # - criacao_features_azure_devops
  # - criacao_tarefas_azure_devops
  # - revisor_tarefas
