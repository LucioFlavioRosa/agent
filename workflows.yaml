# workflows.yaml
# Removidos os workflows Azure conforme instruções
modernizacao:
  description: Modernização de código
  steps:
    - agent: modernizador
      status_update: starting
      requires_approval: true
implementacao:
  description: Implementação de mudanças
  steps:
    - agent: implementador
      status_update: starting
      requires_approval: true
criacao_epicos:
  description: Criação de épicos a partir de transcrição
  steps:
    - agent: epico_generator
      status_update: starting
      requires_approval: true
