# Configurações do Azure Key Vault
# URL do seu Azure Key Vault onde os segredos são armazenados
KEY_VAULT_URL=https://<seu-vault>.vault.azure.net/

# Configurações do Redis
# URL de conexão com o Redis para armazenamento de jobs
# Formato: rediss://:<senha>@<host>:<porta>
REDIS_URL=rediss://:<senha>@<host>:<porta>

# Configurações do Azure AI Search (para RAG)
# Endpoint do seu serviço Azure AI Search
AI_SEARCH_ENDPOINT=https://<seu-search>.search.windows.net

# Nome do índice no Azure AI Search
AI_SEARCH_INDEX_NAME=<nome-index>

# Nome do modelo de embeddings da OpenAI
AZURE_OPENAI_EMBEDDING_MODEL_NAME=text-embedding-3-large

# NOTA: Os segredos reais (tokens e chaves de API) são obtidos do Key Vault
# e não devem ser definidos diretamente neste arquivo.
# No Azure App Service, estes valores devem ser configurados em App Settings.
#
# Segredos necessários no Key Vault:
# - githubapi: Token de acesso ao GitHub
# - openaiapi: Chave da API da OpenAI
# - aisearchapi: Chave da API do Azure AI Search
# - ANTHROPICAPIKEY: Chave da API da Anthropic (opcional)